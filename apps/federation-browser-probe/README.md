# Federation Browser probe

여러 source의 bounded geometry range를 source slot과 alignment가 보존된 하나의
derived display projection으로 구성해 실제 Chromium WebGL2 first-frame,
aggregate CPU/GPU budget과 cleanup을 검증하는 qualification surface입니다.

현재 제품급 fixture는 repository-generated IFC 두 개와 on-demand CC BY 4.0
`A Beautiful Game` GLB 하나를 동시에 사용합니다. 원본 GLB는 Git·package·
release에 포함하지 않으며, 이 probe는 physical GPU·survey datum·실제 사용자
수요·Spatial authority 또는 production federation을 주장하지 않습니다.
